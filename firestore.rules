rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Chỉ người đã đăng nhập (kể cả ẩn danh) mới đọc/ghi được
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
